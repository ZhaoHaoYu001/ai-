{
  "name": "fluentloop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --test && node --check src/app.js && node --check server.js"
  }
}
